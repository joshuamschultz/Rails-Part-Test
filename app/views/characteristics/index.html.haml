%h1 Listing characteristics

%table
  %tr
    %th Name
    %th Symbol
    %th Two sided
    %th Continuous
    %th
    %th
    %th

  - @characteristics.each do |characteristic|
    %tr
      %td= characteristic.name
      %td= characteristic.symbol
      %td= characteristic.two_sided
      %td= characteristic.continuous
      %td= link_to 'Show', characteristic
      %td= link_to 'Edit', edit_characteristic_path(characteristic)
      %td= link_to 'Destroy', characteristic, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Characteristic', new_characteristic_path
